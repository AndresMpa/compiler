REM.  DEF DIM LET READ
 
DIM prueba =  6;
DEF prueba = 10;
LET prueba = 80;
READ X,Z,Y,var;

REM.  SIN COS TAN EXP ATN COT 
IF SIN(COS(0)):
   TAN(0)
   
IF EXP(2)=4;
	RETURN 0
	
IF ATN(0)=0.78539;
	RETURN SALIR
	
DEF COT(x):
    RETURN 1.0/TAN(x)
    
DEF SEC(x):
    return 1.0/COS(x)

IF Var: 5;
   RETURN 10
   
REM . GOSUB GOTO THEN CONDICIONAL
DEF tryToGo(variable):
	IF variable:
		GOTO 20
	THEN
		IF variable == 10;
		GOSUB 1
	END

REM. CONDICIONAL 

IF PRUEBA = 1
	PRINT ('PRUEBA INVALIDA');
THEN:
	RETURN PRUEBA;
	
IF EXP(2)=4
	RETURN 0
	


